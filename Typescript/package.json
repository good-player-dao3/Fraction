{
    "scripts": {
        "build": "tsc",
        "prepublishOnly": "npm run build",
        "test": "jest",
        "test:watch": "jest --watchAll"
    },
    "description": "一个简单的分数类库，支持加减乘除、幂运算和开方运算。\nA simple fraction library that supports addition, subtraction, multiplication, division, exponentiation and root extraction.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "name": "fraction-tool",
    "version": "1.0.1",
    "files": [
        "dist",
        "LICENSE",
        "README.md"
    ],
    "dependencies": {
        "fraction-tool": "^1.0.0"
    },
    "devDependencies": {
        "@types/jest": "^29.0.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.0.0"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node",
        "testMatch": [
            "**/test/**/*.test.ts"
        ]
    }
}
